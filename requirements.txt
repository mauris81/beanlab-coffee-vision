# Dependências da plataforma (testadas com Python 3.14 em 25/09/2026).
# Versões fixas para que todo computador instale exatamente o mesmo conjunto.
# Para desenvolver (rodar testes), use requirements-dev.txt.

# Servidor web e banco de dados
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.1.1
Werkzeug==3.1.8
Flask-Migrate==4.1.0
alembic==1.20.0
# Servidor de produção (vários celulares ao mesmo tempo); funciona no Windows
waitress==3.0.2

# Configuração (listas de classes em taxonomias/*.yaml)
PyYAML==6.0.3

# Processamento de imagem e segmentação
numpy==2.5.3
opencv-python-headless==5.0.0.93
scikit-image==0.26.0
scipy==1.18.1
Pillow==12.3.0
